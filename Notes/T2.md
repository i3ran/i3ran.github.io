Looking at this problem, I need to find the maximum total distance that game pieces (1s) can travel when each can move at most once, pieces can't jump over or occupy each other, and they can move left or right.

Let me think through this step by step:

The key insight is that pieces can only move into empty spaces (0s), and they can't cross each other. This means the relative order of pieces must be preserved. We're essentially trying to move pieces into empty positions to maximize the total distance.

The optimal strategy is to:

1. Identify all pieces and their positions
2. Identify all empty positions
3. Match pieces to empty positions (while preserving order) to maximize total distance

Since pieces can move left or right, and they maintain their relative order, this becomes a matching problem where we want to maximize the sum of absolute differences between original positions and final positions.

Let me write the solution:

```java
class Solution {
    public int solution(int[] board) {
        int n = board.length;
        
        // Get positions of pieces (1s) and empty spaces (0s)
        java.util.ArrayList<Integer> pieces = new java.util.ArrayList<>();
        java.util.ArrayList<Integer> empties = new java.util.ArrayList<>();
        
        for (int i = 0; i < n; i++) {
            if (board[i] == 1) {
                pieces.add(i);
            } else {
                empties.add(i);
            }
        }
        
        int m = pieces.size();
        int e = empties.size();
        
        // If there are no pieces or no empty spaces, no moves possible
        if (m == 0 || e == 0) {
            return 0;
        }
        
        // We need to match pieces to empty positions
        // Each piece can move to at most one empty position
        // We want to maximize total distance
        // This is a maximum weight matching in a bipartite graph
        // with the constraint that order must be preserved
        
        // DP approach: dp[i][j] = max distance using first i pieces and first j empty positions
        int[][] dp = new int[m + 1][e + 1];
        
        for (int i = 1; i <= m; i++) {
            for (int j = 1; j <= e; j++) {
                // Option 1: Don't use this empty position
                dp[i][j] = dp[i][j - 1];
                
                // Option 2: Move piece i-1 to empty position j-1
                // But we need to ensure we've used i-1 pieces with j-1 empty positions
                int distance = Math.abs(pieces.get(i - 1) - empties.get(j - 1));
                dp[i][j] = Math.max(dp[i][j], dp[i - 1][j - 1] + distance);
            }
        }
        
        return dp[m][e];
    }
}
```

Wait, but this DP might not capture the full picture. The pieces don't have to move to specific empty positions - they can move to any position that becomes empty after other pieces move.

Actually, let me reconsider. The key insight is that pieces can only move into empty spaces, and since they maintain their order, the final configuration will have the same number of pieces, and they'll occupy some positions.

A simpler approach: Since pieces maintain their order, the optimal strategy is to move each piece to the farthest possible position it can reach without crossing other pieces. The total distance is maximized when pieces move to extreme positions (some left, some right).

Let me think differently:

For each piece, we can decide whether it moves left or right. The maximum total distance is achieved by:

1. Moving some pieces to the leftmost available positions
2. Moving some pieces to the rightmost available positions

Actually, let me reconsider the problem more carefully. Since pieces can move left or right and can't cross, and each moves at most once, the optimal strategy is:

1. We have m pieces and n-m empty spaces
2. Each piece can move to any position as long as the relative order is preserved
3. The total distance is sum of |original_position - final_position|

We want to maximize this sum. The optimal is to move pieces to the extremes - some to the leftmost positions, some to the rightmost positions.

Let me try a different DP approach where I consider the final positions of pieces:

```java
class Solution {
    public int solution(int[] board) {
        int n = board.length;
        java.util.ArrayList<Integer> pieces = new java.util.ArrayList<>();
        
        for (int i = 0; i < n; i++) {
            if (board[i] == 1) {
                pieces.add(i);
            }
        }
        
        int m = pieces.size();
        if (m == 0 || m == n) {
            return 0;
        }
        
        // DP: maximum total distance for first i pieces
        // where the i-th piece ends at position j (or beyond)
        // Actually, let's use a different approach
        
        // We want to assign each piece to a final position
        // The final positions must be increasing (preserve order)
        // and can be any positions in [0, n-1]
        // We want to maximize sum of |original - final|
        
        // This is like: we have m pieces at positions p[i], 
        // we need to choose m positions q[i] with q[0] < q[1] < ... < q[m-1]
        // maximizing sum |p[i] - q[i]|
        
        // Since we can move to any position, the optimal q[i] will be
        // as far from p[i] as possible while maintaining order
        
        // Let's use DP: dp[i][j] = max distance for first i pieces
        // using positions up to j (inclusive)
        int[][] dp = new int[m + 1][n + 1];
        
        for (int i = 1; i <= m; i++) {
            for (int j = i; j <= n - (m - i); j++) {
                // Position j-1 (0-indexed) is the final position for piece i-1
                int pos = j - 1;
                int distance = Math.abs(pieces.get(i - 1) - pos);
                
                // Either don't use position j-1, or use it as final position for piece i-1
                dp[i][j] = Math.max(dp[i][j - 1], dp[i - 1][j - 1] + distance);
            }
        }
        
        return dp[m][n];
    }
}
```

Wait, this doesn't quite work either because I need to consider that pieces can move to positions that are currently occupied by other pieces (after those pieces move).

Actually, the simplest correct approach: Since pieces maintain their relative order, and each can move at most once, the optimal strategy is:

1. Move pieces on the left to the left (if there are empty spaces on the left)
2. Move pieces on the right to the right (if there are empty spaces on the right)
3. Or a combination

The key insight is that the maximum total distance is achieved by moving pieces to the extremes. For each piece, we can think about whether it should move left or right.

Let me try a different approach using greedy:

Actually, I think the optimal strategy is:

· The leftmost k pieces should move as far left as possible
· The rightmost (m-k) pieces should move as far right as possible

Where k is chosen to maximize the total distance.

Let me implement this:

```java
class Solution {
    public int solution(int[] board) {
        int n = board.length;
        java.util.ArrayList<Integer> pieces = new java.util.ArrayList<>();
        
        for (int i = 0; i < n; i++) {
            if (board[i] == 1) {
                pieces.add(i);
            }
        }
        
        int m = pieces.size();
        if (m == 0 || m == n) {
            return 0;
        }
        
        // Try all possible splits: k pieces move left, m-k move right
        int maxTotal = 0;
        
        for (int k = 0; k <= m; k++) {
            int total = 0;
            // First k pieces move left to positions 0, 1, ..., k-1
            for (int i = 0; i < k; i++) {
                total += pieces.get(i) - i;
            }
            
            // Remaining m-k pieces move right to positions n-(m-k), ..., n-1
            for (int i = k; i < m; i++) {
                total += (n - (m - i)) - pieces.get(i);
            }
            
            maxTotal = Math.max(maxTotal, total);
        }
        
        return maxTotal;
    }
}
```

Let me test this with the example: board = [1,1,0,0,1]

· pieces = [0,1,4], m = 3, n = 5

k=0: pieces move right to positions 2,3,4: (2-0)+(3-1)+(4-4) = 2+2+0 = 4
k=1: pieces move left: 0-0=0, right: (3-1)+(4-4)=2+0=2, total=2
k=2: left: (0-0)+(1-1)=0, right: (4-4)=0, total=0
k=3: left: (0-0)+(1-1)+(4-2)=0+0+2=2

Max = 4, which matches the example.

Let me test a few more cases:

[1,0,1,0,1] -> pieces = [0,2,4], n=5
k=0: move right: (3-0)+(4-2)+(? wait, positions would be 2,3,4) 
Actually let me recalculate: positions 2,3,4 for pieces at 0,2,4
distances: (2-0)+(3-2)+(4-4)=2+1+0=3

Wait this doesn't seem right. Let me reconsider:

For k=0, all pieces move right to positions n-m, n-m+1, ..., n-1 = 2,3,4
distances: |0-2| + |2-3| + |4-4| = 2+1+0 = 3

For k=3, all pieces move left to positions 0,1,2
distances: |0-0| + |2-1| + |4-2| = 0+1+2 = 3

For k=1: first piece left to 0: |0-0|=0, remaining right to 3,4: |2-3|+|4-4|=1+0=1, total=1

For k=2: first two left to 0,1: |0-0|+|2-1|=0+1=1, remaining right to 4: |4-4|=0, total=1

Max = 3

Is 3 the correct answer? Let me think: Can we do better?
Original: [1,0,1,0,1]
If we move piece at 2 to 1: [1,1,0,0,1]
Then move piece at 0 to 2: [0,1,1,0,1] -> wait, piece at 2 moved to 1, so position 2 is empty now, piece at 0 can move to 2: distance 2
Total: |2-1| + |0-2| = 1+2 = 3

Or we could move piece at 4 to 3: [1,0,1,1,0]
Then move piece at 2 to 4: [1,0,0,1,1] -> distance 2
Total: |4-3| + |2-4| = 1+2 = 3

Or move piece at 0 to 1: [0,1,1,0,1]
Then move piece at 4 to 3: [0,1,1,1,0]
Then move piece at 2 to 4: [0,1,0,1,1] -> wait, piece at 2 moves to 4: distance 2, total=1+1+2=4

Wait, that's 4! Let me verify:
Start: [1,0,1,0,1]
Step 1: Move piece at 0 to 1: [0,1,1,0,1], distance 1
Step 2: Move piece at 4 to 3: [0,1,1,1,0], distance 1
Step 3: Move piece at 2 to 4: [0,1,0,1,1], distance 2
Total: 4

But wait, pieces can be moved in any order, and each piece at most once. In this sequence, I moved piece at 0, then piece at 4, then piece at 2. But piece at 2 started at position 2, and after step 1, position 2 is still occupied by that piece. In step 3, I moved it to position 4. This is valid because each piece moves at most once.

So the maximum is actually 4, not 3. My greedy approach of moving some pieces left and some right doesn't capture this because we can move pieces multiple steps through a chain.

Let me reconsider. The pieces maintain their relative order, and we want to maximize the sum of distances. This is equivalent to finding the maximum sum of |p[i] - q[i]| where q is a strictly increasing sequence of length m with values in [0, n-1].

Since the absolute value function is convex, the maximum will be achieved at extreme points. The extreme points of the feasible region are when q[i] are as far from p[i] as possible.

Actually, I think the optimal strategy is to choose some pieces to move left and some to move right, but the pieces that move left should be the leftmost ones, and the pieces that move right should be the rightmost ones. The pieces in the middle might stay in place or move slightly.

Let me reconsider with the example [1,0,1,0,1]:

· p = [0,2,4]
· If we choose k pieces to move left and m-k to move right:
  k=0: q = [2,3,4], distances: 2+1+0=3
  k=1: q = [0,3,4], distances: 0+1+0=1
  k=2: q = [0,1,4], distances: 0+1+0=1
  k=3: q = [0,1,2], distances: 0+1+2=3
  But we found a total distance of 4 with q = [1,3,4]? Wait, q = [1,3,4] gives distances: |0-1|+|2-3|+|4-4| = 1+1+0 = 2, not 4.

Let me recalculate the chain I found:
Start: [1,0,1,0,1]
Step 1: Move piece at 0 to 1: [0,1,1,0,1], piece at 0 now at 1, distance 1
Step 2: Move piece at 4 to 3: [0,1,1,1,0], piece at 4 now at 3, distance 1
Step 3: Move piece at 2 to 4: [0,1,0,1,1], piece at 2 now at 4, distance 2
Total: 4

Final configuration: [0,1,0,1,1]
Pieces at positions: 1,3,4
Original positions: 0,2,4
Distances: |0-1| + |2-3| + |4-4| = 1+1+0 = 2

Wait, that's only 2! I miscalculated. The total distance is 2, not 4.

Let me recalculate properly:

· Piece at 0 moves to 1: distance 1
· Piece at 4 moves to 3: distance 1
· Piece at 2 moves to 4: distance 2
  Total: 1+1+2 = 4

But wait, when piece at 2 moves to 4, position 4 is occupied by the piece that moved from 4 to 3. So piece at 2 can move to position 4 because it's empty.

Final configuration: [0,1,0,1,1]
Pieces at: 1,3,4
Distances: |0-1| + |2-3| + |4-4| = 1+1+0 = 2

But the sum of individual distances is 1+1+2 = 4. How can the sum of individual distances be different from the sum of final position differences?

Because the piece at 2 moved to 4, but the piece that was at 4 moved to 3. So the total distance traveled is |0-1| + |4-3| + |2-4| = 1+1+2 = 4. The final positions are 1,3,4, and the sum of |original - final| is |0-1| + |2-3| + |4-4| = 1+1+0 = 2. These are different because the piece at 4 and the piece at 2 swapped relative to their original order? No, the order is preserved: original [0,2,4] -> final [1,3,4], which is strictly increasing.

Wait, the sum of |original - final| for the matching [0->1, 2->3, 4->4] is 2. But the actual moves were: 0->1, 4->3, 2->4. The final positions are 1,3,4, so the matching is [0->1, 2->4, 4->3]? No, the piece at 2 moves to 4 and the piece at 4 moves to 3, so the matching is [0->1, 2->4, 4->3]. But this matching is not order-preserving because 2->4 and 4->3 would mean the piece originally at 4 ends up at 3 which is to the left of where the piece at 2 ends up (position 4). That violates the order-preserving constraint!

Ah, I see the issue. In my step-by-step moves, I violated the constraint that pieces can't cross each other. Let me check:
Start: positions 0,2,4
Step 1: piece at 0 moves to 1: positions 1,2,4
Step 2: piece at 4 moves to 3: positions 1,2,3
Step 3: piece at 2 moves to 4: positions 1,3,4

In step 3, piece at 2 moves to 4, crossing over the piece at 3? No, the piece at 3 is at position 3, and the piece at 2 moves to 4, which is to the right of position 3. So it doesn't cross. The order is preserved: 1 < 3 < 4.

But the matching is: original [0,2,4] -> final [1,3,4]
This is order-preserving: 0<2<4 and 1<3<4.

Wait, but the piece at 2 moved to 4, and the piece at 4 moved to 3. This means the piece originally at 4 is now at 3, which is to the left of where the piece originally at 2 is (position 4). But the original order was [0,2,4] and the final order is [1,3,4], which is still increasing. The matching [0->1, 2->4, 4->3] is not order-preserving, but the actual matching is [0->1, 2->4, 4->3]... wait, no. The final positions are 1,3,4. The piece at 1 came from 0, the piece at 3 came from 4, and the piece at 4 came from 2. So the matching is [0->1, 2->4, 4->3]. This is NOT order-preserving because 2->4 and 4->3 means the order is reversed for these two pieces.

But wait, I said the order is preserved in the final configuration: 1 < 3 < 4. The pieces at positions 1,3,4 correspond to original pieces at 0,4,2 respectively. The order of original pieces is 0,2,4, but the order of corresponding final pieces is 1,3,4. The piece originally at 2 is now at position 4, and the piece originally at 4 is now at position 3. This means the relative order of these two pieces has been swapped, which is not allowed!

So my step-by-step move sequence is invalid because it caused two pieces to cross each other. Let me verify:

· Piece at 2 moves to 4
· Piece at 4 moves to 3

When piece at 4 moves to 3, it's moving left. When piece at 2 moves to 4, it's moving right. For them to swap order, they would have to cross each other at some point. The piece at 4 moving to 3 would pass through position 2? No, it moves from 4 to 3, which is one step left. The piece at 2 moves from 2 to 4, which goes through positions 2->3->4. At some point, they would both be at position 3 (or cross each other). This is not allowed.

So the maximum total distance for [1,0,1,0,1] is actually 3, not 4.

Let me go back to my greedy approach. For [1,0,1,0,1], my greedy approach gave max = 3, which I think is correct.

Let me test a few more cases to verify my approach:

1. [1,1,0,0,1] -> pieces = [0,1,4], n=5
   k=0: [2,3,4] -> (2-0)+(3-1)+(4-4) = 2+2+0 = 4
   k=1: [0,3,4] -> (0-0)+(3-1)+(4-4) = 0+2+0 = 2
   k=2: [0,1,4] -> (0-0)+(1-1)+(4-4) = 0
   k=3: [0,1,2] -> (0-0)+(1-1)+(4-2) = 2
   Max = 4 ✓
2. [1,0,0,1,0] -> pieces = [0,3], n=5
   k=0: [3,4] -> (3-0)+(4-3) = 3+1 = 4
   k=1: [0,4] -> (0-0)+(4-3) = 1
   k=2: [0,1] -> (0-0)+(3-1) = 2
   Max = 4
   Can we do 4? Move piece at 0 to 3 (distance 3), piece at 3 to 4 (distance 1), total=4. Final: [0,0,0,1,1]. Valid? Start: [1,0,0,1,0]. Move piece at 3 to 4: [1,0,0,0,1]. Move piece at 0 to 3: [0,0,0,1,1]. Valid! Total=4.
3. [1,0,0,0,1] -> pieces = [0,4], n=5
   k=0: [3,4] -> (3-0)+(4-4) = 3+0 = 3
   k=1: [0,4] -> (0-0)+(4-4) = 0
   k=2: [0,1] -> (0-0)+(4-1) = 3
   Max = 3
   Can we do better? Move piece at 0 to 3 (distance 3), piece at 4 stays at 4. Total=3. Or piece at 4 to 1 (distance 3), piece at 0 stays at 0. Total=3. Max=3.
4. [1,0,1,0,0] -> pieces = [0,2], n=5
   k=0: [3,4] -> (3-0)+(4-2) = 3+2 = 5
   k=1: [0,4] -> (0-0)+(4-2) = 2
   k=2: [0,1] -> (0-0)+(2-1) = 1
   Max = 5
   Can we get 5? Move piece at 0 to 3 (distance 3), piece at 2 to 4 (distance 2), total=5. Start: [1,0,1,0,0]. Move piece at 0 to 3: [0,0,1,1,0]. Move piece at 2 to 4: [0,0,0,1,1]. Valid! Total=5.

My greedy approach seems to work for these cases. Let me finalize the solution:

```java
class Solution {
    public int solution(int[] board) {
        int n = board.length;
        java.util.ArrayList<Integer> pieces = new java.util.ArrayList<>();
        
        for (int i = 0; i < n; i++) {
            if (board[i] == 1) {
                pieces.add(i);
            }
        }
        
        int m = pieces.size();
        if (m == 0 || m == n) {
            return 0;
        }
        
        int maxTotal = 0;
        
        // Try all possible splits: k pieces move left, m-k move right
        for (int k = 0; k <= m; k++) {
            int total = 0;
            
            // First k pieces move left to positions 0, 1, ..., k-1
            for (int i = 0; i < k; i++) {
                total += pieces.get(i) - i;
            }
            
            // Remaining m-k pieces move right to positions n-(m-k), ..., n-1
            for (int i = k; i < m; i++) {
                total += (n - (m - i)) - pieces.get(i);
            }
            
            maxTotal = Math.max(maxTotal, total);
        }
        
        return maxTotal;
    }
}
```

Let me double-check with one more case where pieces are already in optimal positions:

[1,1,1,0,0] -> pieces = [0,1,2], n=5
k=0: [2,3,4] -> (2-0)+(3-1)+(4-2) = 2+2+2 = 6
k=1: [0,3,4] -> (0-0)+(3-1)+(4-2) = 0+2+2 = 4
k=2: [0,1,4] -> (0-0)+(1-1)+(4-2) = 2
k=3: [0,1,2] -> (0-0)+(1-1)+(2-2) = 0
Max = 6

Is 6 valid? Move piece at 0 to 2 (distance 2), piece at 1 to 3 (distance 2), piece at 2 to 4 (distance 2). Start: [1,1,1,0,0]. Move piece at 0 to 2: [0,1,1,0,1]. Move piece at 1 to 3: [0,0,1,1,1]. Move piece at 2 (now at position 2) to 4: [0,0,0,1,1]. Final: [0,0,0,1,1]. Total=6. Valid!

The solution seems correct. The key insight is that the optimal strategy is to move some leftmost pieces to the leftmost positions and some rightmost pieces to the rightmost positions, and the total distance is maximized by choosing the split that gives the maximum sum.
